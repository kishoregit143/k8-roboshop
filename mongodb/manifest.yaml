apiversion: app/v1
kind: Deployment
metadata:
 name: mongodb
 namespace: roboshop
 #this labels are replecaset labels
 labels:
   project: toboshop
   component: mongodb
   tier: db
spec:
 replicas: 1
 selector:
 # this lablels used to  select the pods
  matchlabels:
   project: toboshop
   component: mongodb
   tier: db
   #pod defention
  templects:
    metadata:
      labels:
      project: toboshop
      component: mongodb
      tier: db
    spec:
      containers:
      - name: mongodb
        image: pullfrom dockerhub
        